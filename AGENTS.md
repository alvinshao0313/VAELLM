# AGENTS.md

## 基本原则

* 使用第一性原理理解需求，从问题本身出发，不要机械套模板。
* 简单任务直接做，不要扩展成复杂方案。
* 用简单、直接的中文沟通。
* 不要使用 P0/P1/P2 这类术语。
* 已经确认过的信息，后续不要反复追问。
* 发现问题要直接说明，不要用兜底逻辑、兼容补丁或启发式后处理掩盖问题。
* 不清楚时先问用户，不要自己猜实现方案。

## 禁止行为

* 禁止自行创建新的 git worktree。
* 禁止自行切换分支。
* 禁止添加用户没有要求的新功能。
* 禁止顺手重构无关代码。
* 禁止为了让测试通过而添加临时 mock、stub 或无关依赖。
* 禁止用降级方案、兼容补丁、兜底逻辑替代正确实现。

## 环境规则

本项目所有 Python 命令、测试和训练脚本都必须在 `bitvae` conda 环境中运行。

不要自动使用：

```bash
conda run -n bitvae ...
```

默认用户已经手动进入 `bitvae` 环境。

如果需要确认环境，只能检查当前环境：

```bash
echo $CONDA_DEFAULT_ENV
which python
python -c "import sys; print(sys.executable)"
```

如果当前环境不是 `bitvae`，必须停止操作，并提醒用户先执行：

```bash
conda activate bitvae
```

不要依赖 shell 默认的 `python` 环境。

## 修改代码规则

* 只修改用户要求相关的代码。
* 保持最短路径实现，不要过度设计。
* 修改前先看清楚完整调用链：

  * shell 脚本如何传参；
  * CLI 参数如何解析；
  * Python 入口如何调用主流程；
  * 数据、模型、训练、压缩、保存逻辑如何连接；
  * ckpt 保存格式是否会影响后续加载。
* 如果涉及训练流程、ckpt 格式、数据格式或参数含义，必须确认完整链路后再改。

## shell 脚本规则

`.sh` 文件只负责三件事：

* 设置必要环境变量；
* 写清楚本次实验的 CLI 参数；
* 调用 Python 入口脚本。

`.sh` 文件不要写大量业务逻辑。

`.sh` 文件中不要使用：

```bash
conda run -n bitvae python xxx.py
```

应该直接使用：

```bash
python xxx.py
```

前提是当前 shell 已经处于 `bitvae` 环境。

固定实验超参数必须直接写在 Python 命令里，不要包装成中间 shell 变量。

不要写成：

```bash
SEED="${SEED:-42}"

python train.py \
  --wa_mse_calib_seed "${SEED}"
```

应该写成：

```bash
python train.py \
  --wa_mse_calib_seed 42
```

只有下面几类情况允许使用 shell 变量：

* 路径变量，例如 `MODEL_PATH`、`DATA_DIR`、`OUTPUT_DIR`；
* 同一个值在脚本里被多次复用；
* 用户明确要求该参数可以通过环境变量覆盖。

不要把每个 CLI 参数都包装成 shell 变量。

`.sh` 文件的目标是清楚记录一次实验配置，不是写成通用参数模板。

## 代码拆分规则

不要把大量逻辑塞进一个脚本。

如果一个脚本同时包含参数解析、数据读取、模型定义、训练循环、压缩算法、ckpt 保存和评估逻辑，应该拆分。

入口脚本要薄，核心逻辑要进入可复用模块。

## 测试规则

* 只有用户明确要求，或者本次修改必须验证时，才执行最小必要测试。
* 测试必须在 `bitvae` 环境中运行。
* 不要为了测试添加临时依赖。
* 不要为了测试修改正式逻辑。
* 临时测试脚本用完后必须删除。
* 如果没有测试，直接说明原因。

## 完成后输出

每次完成修改后，简要说明：

* 改了哪些文件；
* 每个文件改了什么；
* 为什么这样改；
* 是否运行了测试；
* 测试结果是什么；
* 如果没有测试，说明原因。

不要输出冗长背景介绍。
不要重复用户已经知道的信息。
